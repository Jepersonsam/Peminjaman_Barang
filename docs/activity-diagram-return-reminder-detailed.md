# Activity Diagram - Notifikasi Pengingat Pengembalian Barang (Detailed)

## Format Text-Based Activity Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ADMIN SWIMLANE                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    [Start]
      │
      ▼
┌─────────────────────┐
│ Access Dashboard    │  Admin membuka halaman Manajemen Peminjaman
│ Manajemen           │
│ Peminjaman          │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Click Tombol        │  Admin klik "Kirim Notifikasi" pada borrowing
│ Kirim Notifikasi    │
└──────────┬──────────┘
           │
           │ (Trigger)
           │
           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND SWIMLANE                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│ Receive Click       │  Frontend menerima event klik
│ Event               │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Send POST Request   │  POST /api/borrowings/{id}/send-return-reminder
│                     │  Headers: Authorization: Bearer {token}
│                     │  Body: (empty)
└──────────┬──────────┘
           │
           │ (HTTP Request)
           │
           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          BACKEND SWIMLANE                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│ Receive POST        │  Backend menerima request
│ Request             │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Validate            │  Cek token & permission (can:edit-borrowing)
│ Authentication      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Find Borrowing      │  Borrowing::with(['user', 'item'])->findOrFail($id)
│ by ID               │
└──────────┬──────────┘
           │
           ▼
        ┌──┴──┐
        │     │
    ┌───▼───┐ │
    │ Exists│ │
    └───┬───┘ │
        │     │
    ┌───▼───┐ │
    │  Yes  │ │
    └───┬───┘ │
        │     │
        └─────┘
           │
           ▼
┌─────────────────────┐
│ Validate Status     │  Cek: is_returned = false
│                     │  Cek: approval_status = approved
└──────────┬──────────┘
           │
           ▼
        ┌──┴──┐
        │     │
    ┌───▼───┐ │
    │ Valid │ │
    └───┬───┘ │
        │     │
        └─────┘
           │
           ▼
┌─────────────────────┐
│ Check User & Email  │  Cek: user exists && email exists
└──────────┬──────────┘
           │
           ▼
        ┌──┴──┐
        │     │
    ┌───▼───┐ │
    │ Exists│ │
    └───┬───┘ │
        │     │
        └─────┘
           │
           ▼
┌─────────────────────┐
│ Calculate Days      │  $today = Carbon::today()
│ Remaining           │  $returnDate = Carbon::parse(return_date)->startOfDay()
│                     │  $daysRemaining = $today->diffInDays($returnDate, false)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Create Notification │  new ReturnReminderNotification($borrowing, $daysRemaining)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Send Email          │  $user->notify($notification)
│                     │  Email dikirim ke user email
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Log Result          │  Log::info/error jika ada error
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Return 200 Success  │  Response JSON:
│                     │  {
│                     │    "message": "Notifikasi pengingat berhasil dikirim.",
│                     │    "data": { ... }
│                     │  }
└──────────┬──────────┘
           │
           │ (HTTP Response)
           │
           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FRONTEND SWIMLANE                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│ Receive Response    │  Frontend menerima response JSON
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Display Message     │  Tampilkan success/error message
│ to Admin            │  (Toast notification / Alert)
└──────────┬──────────┘
           │
           │ (Update UI)
           │
           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           ADMIN SWIMLANE                                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│ View Result         │  Admin melihat pesan success/error
│                     │  Email sudah terkirim ke peminjam
└──────────┬──────────┘
           │
           ▼
        [End]
```

## Error Paths

### Path 1: Borrowing Not Found
```
Find Borrowing → No → Return 404 → Frontend → Display Error
```

### Path 2: Invalid Status
```
Validate Status → No → Return 400 → Frontend → Display Error
```

### Path 3: User/Email Not Found
```
Check User & Email → No → Return 404 → Frontend → Display Error
```

### Path 4: Email Send Failed
```
Send Email → Error → Log Error → Return 500 → Frontend → Display Error
```

## Email Content Based on Days Remaining

### Days Remaining > 0 (Masih ada hari tersisa)
```
Subject: Pengingat Pengembalian Barang - {Item Name}
Message: "Akan jatuh tempo dalam X hari"
```

### Days Remaining = 0 (Hari yang sama)
```
Subject: Pengingat Pengembalian Barang - {Item Name}
Message: "Hari ini adalah tanggal pengembalian"
```

### Days Remaining < 0 (Sudah terlambat)
```
Subject: Pengingat Pengembalian Barang - {Item Name}
Message: "PERINGATAN: Sudah melewati tanggal pengembalian!"
Details: "Terlambat: X hari"
```

